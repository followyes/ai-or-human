{
  "name": "ai-or-human",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "scripts": {
    "images": "node scripts/generate-image-list.js",
    "audit": "node --check js/game.js && node --check js/history.js && node --check scripts/generate-image-list.js && node --check scripts/build-site.js && node --check scripts/audit-project.js && node scripts/audit-project.js",
    "build": "npm run images && npm run audit && node scripts/build-site.js"
  }
}
